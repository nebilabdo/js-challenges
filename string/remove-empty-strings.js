const words = ["pen", "", "pencil", "", "book"];
const removeEmptyString=words.filter(data=>data)
console.log(removeEmptyString)
//filter only gives the truth part only that means emphty is the false value so it does not returned